import 'package:flutter/material.dart';

class OpenOrdersScreen extends StatelessWidget {
  const OpenOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'This is the open orders page',
      ),
    );
  }
}
